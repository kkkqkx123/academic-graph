V2：
当前column的样式过于单一，不同风格间没有区别。请更新以下内容：

**column样式**
- column边框与内部可以独立选择样式。默认使用同色，内部实心
	- 边框样式：颜色；粗细【外部对齐。适用于内部不是实心时】
	- 内部：网格、斜线、空心、实心等；颜色
- 增加column宽窄、边缘样式(形状：矩形、圆角矩形)等
- 取消风格设置，独立设置每个column的颜色

**新增功能**
- 增加选项：堆积百分比柱形图【柱状图内部加百分比】
输入数据格式：name, (1,2,3), ……
可以选择显示百分比/数值，把元组内的数据放到同一个column中

- 增加分组功能，能够独立为每组添加column
- 增加column间隙选项，分为组内间隙、组外间隙

**数据显示**
- 增加选项：数据标签
- 数值标线增加选项：线条深浅、粗细
- 允许独立设置坐标轴、名称、表头、数据标签的字体、颜色

**背景**
- 增加图表背景颜色选项



V3
**声明：**
之前对数值标签的描述不够清晰，引发了误解，在此重新声明：
- 上方导航新增标签栏，专门管理相关设置
- 数值标签的名称改为标签。移除图表配置栏中的数值标签，将原先的字体、颜色设置也移动到标签栏
- 标签用于提供额外的描述信息。可以针对整个图表，也可以针对某个column，通过属性"作用域"管理
- 可以有多个标签，作用域相互独立。
- 标签应当能够选择作用域，包括global、column1、……。
- 标签应当能够选择位置(图表左侧、右侧、上方、右上、左上)
- 标签应当允许自定义格式，如每个标签后自动换行、智能换行(根据标签内容的长度决定一行几个。相应设置处应当说明具体规则)
具体设计可以参考python的matplotlib中的实现

- 柱状图数据板块中column的填充样式设置为空心后内部应当是空白的，检查当前的实现是否正确

**修正：**
- column分组未实现。另外图表配置中柱状图数据模块就应该分组，新增column时也应当分组
- 当前图表配置处选择堆积百分比柱形图时要求在数值处输入1,2,3等，但无法输入"."。应当增加"新增"按钮允许新增数值，而非要求在数值输入框内输入元组。同时对于新加的值应当设计"删除"的按钮
- 堆积百分比柱形图新增值后应当给出额外的填充颜色设置，保证同一个column内可以独立设置多种内部颜色
- 当前不支持当选择堆积百分比柱形图时在数值输入框输入小数，应当支持小数。
- 当前极简风格为黑边框，内部白色。修改极简风格的描述
- 新增灰度风格，按照数值大小的排序给出不同的灰度
- 当前样式设置的优先级高于独立设置颜色，应当独立设置的颜色优先级更高
- 当前填充颜色、填充样式、边框宽度、column宽度、column形状的设置均无法生效。
- 网格线透明度、网格线粗细未生效
- 数值标签未生效
- 轴配置里补充说明何时为数值，何时为字符串(当前似乎缺省表示字符串？描述不够清晰)
- 未实现第一版需求中设置柱状图上下限的要求。请在图表配置栏的柱状图数据版块增加全局设置

**新增功能：**
- 新增网格线粗细设置
- 预览渲染处点击"重新渲染"时作出提示，以免不知道是否刷新。提示的形式任意，选择容易实现、不需要引入过多新的依赖的方式

**疑问：**
当前项目如何实现数据持久化？使用了数据库吗？
How does the current project achieve data persistence? Is a database used?


V4：
- 页面背景颜色新增透明灰
add transparent gray as new page background color
- 取消标签位置的设置


- 标签取消全局、column的选项。放在文本框中，支持的位置设置改为图表右侧、上方、右上、左上。

分组功能在渲染时未体现

使用堆积百分比柱形图时图表渲染失败
从堆积百分比柱形图切换回普通模式时应当自动移除新增的值，以免出错。

使用堆积百分比柱形图和普通模式时柱状图都无法渲染

V17渲染正常，堆积百分比柱形图不正常

V18堆积百分比柱形图渲染正常，但未区分颜色。目前堆积百分比图的逻辑是直接相加，需要进一步修正

V21柱子宽度生效


全局标签：对所有柱子生效
填充颜色、边框颜色无效
全局数值限制未生效
当前填充颜色、填充样式、边框宽度、column宽度、column形状的设置均无法生效。